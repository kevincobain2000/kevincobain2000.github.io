---
import { Image } from "@astrojs/image/components";
export interface Props {
  src: string;
  alt: string;
}
const { src, alt } = Astro.props as Props;
---

<Image
  aspectRatio="1:1"
  src={src}
  alt={alt}
  format="avif"
/>